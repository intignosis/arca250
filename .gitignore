# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Local secrets (API keys, stream keys); .env.example is the committed template.
.env
.env.local

# Local creative presets stay local; the shipped default and this project's
# own DASHWORLD preset are tracked.
streaming-client/presets/*.json
!streaming-client/presets/default.json
!streaming-client/presets/dashworld.json

# Training footage and generated clips are large and not ours to redistribute.
fal/clips/
fal/out/
fal/reference/

# Editor and OS state
.vscode/
.idea/
.DS_Store
